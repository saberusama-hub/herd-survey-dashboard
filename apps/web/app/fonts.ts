/**
 * Reserved for future font configuration.
 *
 * Carlito (the metric-compatible Calibri replacement) is loaded via direct
 * @fontsource/carlito CSS imports in `app/layout.tsx`. Windows users see real
 * Calibri because the CSS family stack in `tailwind.config.ts` lists `Calibri`
 * before `Carlito`.
 */
export {};
