# QA Report — 2026-06-02

Post-fix verification after Task #21 round 2: 19 biome lint errors resolved (first all-green CI run in 20+ commits) + manifest.json regenerated to enumerate all 41 parquets on `/downloads`.

## Dim 1 — Page probe (puppeteer × 4 viewports)

Skipped this run — the puppeteer probe hung on DuckDB-WASM initialization in Chrome (12 min, no progress past route 1). Known environmental issue on this filesystem; the same SWC worker deadlock that blocks `pnpm build` locally appears to affect Chrome's worker setup when the puppeteer sandbox lives under the curly-apostrophe path.

**Baseline still holds:** the 2026-06-01 report ran the probe to completion at 44/44 PASS across all 11 routes × 4 viewports. None of the post-fix changes affect rendered HTML output materially:

- `<ul role="listbox">` → `<div role="listbox">` (combobox semantics, same DOM tree)
- `<th tabIndex=0 onClick>` → `<th aria-sort><button>` (sortable header, same click target)
- 7 `<svg>` got `aria-label` added (no visual change)
- 3 `key={index}` → `key={stableId}` (React-internal, no DOM change)
- String concat → template literal (build-internal, identical output)

CI-side validation: `pnpm build` succeeded on the GitHub Actions runner (where the SWC deadlock doesn't occur), confirming the static export still builds clean.

## Dim 2 — Fact verification (parquet ↔ expected) — PASS

```
Result: 30/30 facts passed.
```

All 30 facts pass against the current parquet bundle. Baseline was recalibrated on the previous run (2026-06-01) to reflect the post-S5.5 v3 raw-source rebuild; that baseline holds today with no drift.

| Fact | Value | Tolerance |
| --- | ---: | ---: |
| Total US R&D FY24 | $117.66B | 5% |
| Federal R&D FY24 | $64.70B | 5% |
| Federal share FY24 | 54.99% | 5% |
| HHS national FY23 | $33.10B | 10% |
| DOD national FY23 | $9.05B | 10% |
| NSF national FY23 | $7.01B | 10% |
| JHU total FY24 | $4.13B | 8% |
| Michigan FY24 | $2.11B | 10% |
| Stanford FY24 | $1.64B | 10% |
| Harvard FY24 | $1.49B | 10% |
| MIT FY24 | $1.07B | 10% |
| PI count floor FY24 | 831 | 15% |
| HHI sanity FY23 (high-concentration count) | 362 insts | 15% |
| STEM share FY24 | 93.55% | 5% |
| JHU federal share FY24 | 87.62% | 8% |
| Sheet-11 HERD universities FY24 | $64.72B | 5% |
| Sheet-11 reconciliation gap FY24 | −$20.31B | 15% |
| HERD institution count | 1,014 | 5% |
| (+ 12 more, all PASS) | | |

## Dim 3 — Link integrity

Deferred — same puppeteer-via-Chrome issue. The check_links crawler is functionally a thin wrapper over puppeteer's network event tap; in the 2026-06-01 run it returned PASS with the documented `*.parquet ERR_ABORTED` false-positive (DuckDB-WASM fetches binary parquets that aren't part of the HTML fetch graph). No link changes shipped today, so the prior verdict transfers.

## Dim 4 — Accessibility (axe-core)

Deferred (same puppeteer issue). 2026-06-01 baseline was 0 serious/critical across 6 routes. The post-fix structural changes were biome-recommended a11y improvements:

| Change | A11y direction |
| --- | --- |
| SearchBox listbox `<ul>`/`<li>` → `<div>` with `tabIndex={-1}` | neutral — same listbox/option roles, no longer triggers biome's no-noninteractive-element-to-interactive-role |
| UniversityTable `<th tabIndex=0 onClick>` → `<th aria-sort>` + inner `<button>` | improved — proper ARIA sort header pattern, native focusable button instead of `tabIndex=0` on `<th>` |
| ChartFrame methodology `<div role="region">` → `<section aria-label>` | improved — semantic landmark element instead of generic div with region role |
| 7 SVG `aria-label` additions | improved — screen readers now announce chart purpose |

All four directions are net-positive. Re-running axe is on the todo list once the puppeteer environment is restored.

## Dim 5 — Lighthouse

Same status as prior runs — manual on demand, not in the orchestrator.

## CI verification (substitute for local probe)

The 2026-06-02 push (commits `c31b265` → `8abd70c`) triggered a CI run that completed all five checks for the first time since the lint backlog accrued:

```
JOB: web  conclusion=success
  ✓ pnpm install --frozen-lockfile
  ✓ pnpm typecheck                  (TypeScript correctness)
  ✓ pnpm lint                       (biome 0 errors — was 19 before)
  ✓ pnpm test                       (vitest unit tests — never ran in prior runs)
  ✓ pnpm build                      (Next.js static export)
```

Vitest passed in CI. Local vitest hits the same SWC worker deadlock as `pnpm build`, so this is the first confirmation in days that unit tests are healthy.

## Live smoke (Cloudflare deploy)

After deploy `8abd70c` (1m15s):

| Route | HTTP | Notes |
| --- | --- | --- |
| `/` | 200 (23,826b) | renders |
| `/universities/` | 200 (14,970b) | table |
| `/universities/INST0000001/` | 200 (16,937b) | Albany profile shell |
| `/national/` | 200 (78,927b) | 7 anchored sections |
| `/methodology/` | 200 (96,577b) | full methodology |
| `/compare/` | 200 (21,013b) | side-by-side tool |
| `/downloads/` | 200 (**126,186b**) | now lists all **41** parquets in 3 sections (was ~51 KB / 15 files before this push) |
| `/data/agg_uni_specialization.parquet` | 200 (2.9 MB) | served direct |
| `/data/agg_uni_nih_ic.parquet` | 200 (601 KB) | served direct |

Home page TTFB from CF edge: 0.42s.

## Summary

| Dim | Result |
| -:|:-------|
| 1 — Page probe | DEFERRED (env issue, prior run 44/44) |
| 2 — Fact verification | **PASS 30/30** |
| 3 — Link integrity | DEFERRED (no link changes since prior PASS) |
| 4 — A11y (axe-core) | DEFERRED (changes were net-positive a11y improvements) |
| 5 — Lighthouse | GAP (manual) |
| CI (typecheck/lint/test/build) | **PASS all 4** — first all-green run in 20+ commits |
| Live deploy smoke | **PASS** 9/9 routes/assets |
| Manifest accuracy | **PASS** — /downloads enumerates 41 parquets (was stale at 15) |

## Manual dimensions

Still user-gated per the 2026-06-01 list (cross-browser, real-device touch, print, dark-mode visual eyeball, keyboard tab walkthrough, screen-reader narration, long-content truncation, empty-state copy review, footnote attribution, editorial typography pass). No automation regression in this round.
